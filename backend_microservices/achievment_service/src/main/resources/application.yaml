spring:
  application:
    name: achievment_service

  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:mysql://localhost:3306/achievment_service?createDatabaseIfNotExist=true&useUnicode=yes&characterEncoding=UTF-8
    username: kumuditha
    password: kumuditha1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8004
  servlet:
    context-path: /api