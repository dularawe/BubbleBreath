spring:
  application:
    name: parent-registration-service
  
  datasource:
    url: jdbc:mysql://localhost:3306/bubblebreath_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: dilshani@
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: anjanaupe10@gmail.com
    # 16‑character Gmail App Password for the account above (NO SPACES)
    password: lmzaaoyaavchtkkl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8

app:
  mail:
    from: anjanaupe10@gmail.com
    from-name: BubbleBreath

server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    com.bubblebreath.parentregistration: DEBUG
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG